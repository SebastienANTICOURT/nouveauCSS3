
const typeOf = (arg) => {
    return typeof(arg);
}
console.log(typeOf(1));

// const typeOfAll = (tab) => {
//     for (let i = 0; i < tab.length; i++)
//       {
//         console.log(typeof tab[i]);
//       }
//    }
//    console.log(typeOfAll([1, "a", true]));

// ["number", "string", "boolean"]*/


    
/*EXERCICE 2
 Ecris une fonction qui renvoie le type de chaque élément d'un tableau.*/
    
    const typeOfAll = (tab) => {
    for (let i=0; i<0; i++){
    return typeOfAll(tab)}
    }
    console.log(typeOfAll([1, "a", true])); // ["number", "string", "boolean"]